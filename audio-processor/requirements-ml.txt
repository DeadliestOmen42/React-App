# AI Music Generation Dependencies
# For AudioCraft (MusicGen) - Meta's music generation model

torch>=2.0.0
torchaudio>=2.0.0
audiocraft>=1.3.0

# Optional: for better performance
# xformers>=0.0.20  # Faster attention implementation (requires CUDA)
# accelerate>=0.20.0  # Multi-GPU support

# Note: For CPU-only installation:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Note: For GPU (CUDA) installation:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu118
